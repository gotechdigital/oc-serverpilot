# ===================================
#  Form Field Definitions
# ===================================
fields:
    CLIENT_ID:
        label: Client ID
        type: text
        required: true
        span: left
        commentHtml: true
        comment: 'Obtain this from <a href="https://manage.serverpilot.io/#account/api" target="_blank">here</a>.'
    API_KEY:
        label: Api Key
        type: text
        required: true
        span: right
        commentHtml: true
        comment: 'Obtain this from <a href="https://manage.serverpilot.io/#account/api" target="_blank">here</a>.'
tabs:
    fields:
#        sync_data:
#            label: Sync with ServerPilot
#            type: dropdown
#            tab: Options
#            span: left
#            options:
#                realtime: 'Only Manual Sync'
#                everyMinute: 'Run the task every minute'
#                everyFiveMinutes: 'Run the task every five minutes'
#                everyTenMinutes: 'Run the task every ten minutes'
#                everyThirtyMinutes: 'Run the task every thirty minutes'
#                hourly: 'Run the task every hour'
#                daily: 'Run the task every day at midnight'
        preview_domain:
            label: Preview Domain
            commentHtml: true
            comment: 'Use <span style="color: blue;">{APP_NAME}</span> to indicate where are to use App Name. Example: <small>preview.{APP_NAME}.domain.tld</small>'
            type: text
            tab: Options
            placeholder: 'preview.{APP_NAME}.domain.tld'
            span: left
